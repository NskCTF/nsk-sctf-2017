Участникам дан файл ppc100l.py
 Если посмотреть код этой программы, то можно увидеть несколько стадий проверки пароля. Выполнив все условия мы и получим пароль, который и является флагом.
1. if len(flag)==11:  видим что длина должна быть 11 символов
2.  for i in range(4,7):
        if fib(i-3)!=int(flag[i]):
            break
        sum+=int(flag[i])
запущен цикл от с 4 до 7
и каждый iтый элемент сравнивается с выводом функции fib(i-3)
а так же считается сумма элементов типа int. То можем предположить, что с 4 до7 идут числа. 
в верху программы мы видим функцию - функция для вычисления чсла фибоначи по его номеру в последовательности 
fib = lambda n: fib(n - 1) + fib(n - 2) if n > 2 else 1
посмотрев на цикл с условием i-3, видим что нам нужны числа на 1,2,3 местах (отсчет ведется с 0), а это числа 1, 1, 2. Соответсвенно с 4 по 7 символ стоят 112.
3.a=flag[0]
for i in range(1,4):
            a+=flag[i]
тут уже цикл от 1 до4, но при том 0левой элемент уже присвоен. Так как нет указания о типе переменной, то это переменная типа чар, а именно строка. То есть с 0 по 4 элемент у нас идут буквы
b=flag[7]
        for i in range(8,11):
            b+=flag[i]
аналогично для конца пароля
4. if b==a[::-1]:
здесь идет сравнение строки b со строкой а с параметром [::-1]. Этот параметр указывает, что строка зеркально разворачивается. значит если в в начале или в конце слова стоит к примеру abcd, то с другой строны dcba.
5. if a=="flag":
тут видим условие, что a у нас равно flag, следовательно из предыдущего пункта b = galf
6. print("YOU WIN")
                print("CTF{",flag,"}")
тут нам говорят, что мы победили и нашли флаг, значит наш ответ: flag112galf
